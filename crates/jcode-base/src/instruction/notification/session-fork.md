This session was forked (split) from session {{parent}} ({{parent_id}}) by the user. The full conversation above is inherited from that session, but the original agent in {{parent}} is still active and will continue handling whatever request or work was in progress there. Do NOT continue or duplicate that in-flight work here. Treat the next user message as a fresh request in this new forked session, using the inherited conversation only as context.